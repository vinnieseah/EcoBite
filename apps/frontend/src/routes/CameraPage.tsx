import React from 'react';
import {Camera} from '../components/Camera'; // Adjust path if needed

const DropzonePage: React.FC = () => {
  return (
        <Camera />
  );
};

export default DropzonePage;
